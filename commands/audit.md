---
description: 对网站做 AI 可见性体检（国内 + 海外 AI 引擎）并给中文优化清单
argument-hint: "<域名或URL>"
---

以下是 `seogeo audit` 的体检结果：

!`seogeo audit "$ARGUMENTS" --format md`

请据此给一份中文优先级行动清单：按「国内爬虫准入 → 结构 → 内容 → 站外」排序，每条带"预期效果 + 影响哪些引擎 + 怎么验证"。诚实标注边界（反爬站可能被低估、提升数值按区间表述、不承诺确数）。
